(function(){
  function copyText(text,button){
    const done=function(){if(button){const old=button.textContent;button.textContent="Copied";setTimeout(function(){button.textContent=old},1200)}};
    if(navigator.clipboard&&window.isSecureContext){navigator.clipboard.writeText(text).then(done).catch(function(){fallback()});return}
    fallback();
    function fallback(){const ta=document.createElement("textarea");ta.value=text;ta.setAttribute("readonly","");ta.style.position="fixed";ta.style.opacity="0";document.body.appendChild(ta);ta.select();try{document.execCommand("copy");done()}finally{ta.remove()}}
  }
  window.copyToolResult=function(id,button){const el=document.getElementById(id);if(el)copyText(el.textContent.trim(),button)};
  window.loadImage=function(input){return new Promise(function(resolve,reject){const file=input instanceof File?input:(input.files&&input.files[0]);if(!file){reject(new Error("Choose an image."));return}if(file.size>25*1024*1024){reject(new Error("For reliable phone performance, choose an image under 25 MB."));return}const url=URL.createObjectURL(file),im=new Image();const cleanup=function(){URL.revokeObjectURL(url)};im.onload=function(){cleanup();resolve({im:im,file:file})};im.onerror=function(){cleanup();reject(new Error("Could not read that image."))};im.src=url})};
  window.generatePassword=function(){if(!window.crypto||!crypto.getRandomValues){const unavailable=document.getElementById("result");if(unavailable)unavailable.textContent="Secure password generation is not available in this browser.";return}const chars="ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%^&*()_+-=[]{}",len=Math.min(128,Math.max(8,parseInt(document.getElementById("pwLength").value,10)||20)),a=new Uint32Array(1),limit=Math.floor(4294967296/chars.length)*chars.length;let p="";for(let i=0;i<len;i++){let x;do{crypto.getRandomValues(a);x=a[0]}while(x>=limit);p+=chars[x%chars.length]}const result=document.getElementById("result");result.innerHTML="<code id=\"generatedPassword\"></code><br><button type=\"button\" onclick=\"copyToolResult('generatedPassword',this)\">Copy password</button><p class=\"note\">Generated locally with your browser's cryptographic random source. Do not reuse passwords.</p>";document.getElementById("generatedPassword").textContent=p};
  window.formatJson=function(){const input=document.getElementById("jsonInput"),result=document.getElementById("result");try{const v=JSON.parse(input.value);input.value=JSON.stringify(v,null,2);result.innerHTML="<strong>Valid JSON</strong><br><button type=\"button\" onclick=\"copyToolResult('jsonInput',this)\">Copy formatted JSON</button>"}catch(e){result.textContent="Invalid JSON: "+e.message}};
  window.convertCase=function(){const input=document.getElementById("caseInput"),type=document.getElementById("caseType").value,s=input.value;let o=s;if(type==="upper")o=s.toUpperCase();else if(type==="lower")o=s.toLowerCase();else if(type==="title")o=s.toLowerCase().replace(/\b\w/g,function(x){return x.toUpperCase()});else o=s.toLowerCase().replace(/(^|[.!?]\s+)\w/g,function(x){return x.toUpperCase()});input.value=o;document.getElementById("result").innerHTML="Converted.<br><button type=\"button\" onclick=\"copyToolResult('caseInput',this)\">Copy text</button>"};
})();
