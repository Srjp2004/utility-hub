const $=(id)=>document.getElementById(id);const el=(id)=>$(id);const n=(id)=>{const value=$(id).value.trim();return value?Number(value):NaN};
function renderTool(type,id){const el=$(id);const views={percentage:'<label>Value<input id="part" type="number" value="25"></label><label>Total<input id="whole" type="number" value="200"></label><button data-action=\"calcPercentage\">Calculate</button><div id="result" class="result"></div>',discount:'<label>Original price<input id="price" type="number" min="0" value="100"></label><label>Discount (%)<input id="disc" type="number" min="0" max="100" value="20"></label><button data-action=\"calcDiscount\">Calculate</button><div id="result" class="result"></div>',loan:'<label>Loan amount<input id="loanAmount" type="number" min="0" value="10000"></label><label>Annual interest (%)<input id="loanRate" type="number" min="0" value="7.5"></label><label>Number of months<input id="loanMonths" type="number" min="1" value="60"></label><button data-action=\"calcLoan\">Calculate</button><div id="result" class="result"></div>',interest:'<label>Principal<input id="principal" type="number" min="0" value="10000"></label><label>Annual rate (%)<input id="rate" type="number" min="0" value="8"></label><label>Years<input id="years" type="number" min="0" value="3"></label><button data-action=\"calcInterest\">Calculate</button><div id="result" class="result"></div>',age:'<label>Date of birth<input id="dob" type="date"></label><button data-action=\"calcAge\">Calculate age</button><div id="result" class="result"></div>',change:'<label>Original value<input id="oldv" type="number" value="100"></label><label>New value<input id="newv" type="number" value="125"></label><button data-action=\"calcChange\">Calculate</button><div id="result" class="result"></div>',tip:'<label>Bill amount<input id="bill" type="number" min="0" value="100"></label><label>Tip (%)<input id="tipRate" type="number" min="0" value="15"></label><label>People<input id="people" type="number" min="1" value="2"></label><button data-action=\"calcTip\">Calculate</button><div id="result" class="result"></div>',bmi:'<div class="bmi-input-grid"><label>Weight (kg)<input id="kg" type="number" min="0" step="0.1" value="70" inputmode="decimal"></label><label>Height (cm)<input id="cm" type="number" min="0" step="0.1" value="175" inputmode="decimal"></label></div><button data-action=\"calcBmi\">Calculate BMI</button><div id="result" class="result"></div>',date:'<label>Start date<input id="d1" type="date"></label><label>End date<input id="d2" type="date"></label><button data-action=\"calcDate\">Calculate</button><div id="result" class="result"></div>',convert:'<label>Value<input id="uv" type="number" value="1"></label><label>From<select id="uf"><option value="km">Kilometers</option><option value="mi">Miles</option><option value="kg">Kilograms</option><option value="lb">Pounds</option><option value="c">Celsius</option><option value="f">Fahrenheit</option></select></label><label>To<select id="ut"><option value="km">Kilometers</option><option value="mi">Miles</option><option value="kg">Kilograms</option><option value="lb">Pounds</option><option value="c">Celsius</option><option value="f">Fahrenheit</option></select></label><button data-action=\"calcConvert\">Convert</button><div id="result" class="result"></div>',text:'<label>Text<textarea id="tc" rows="8"></textarea></label><button data-action=\"calcText\">Count</button><div id="result" class="result"></div>',json:'<label>JSON<textarea id="jsonInput" rows="10"></textarea></label><button data-action=\"formatJson\">Format & Validate</button><div id="result" class="result"></div>',case:'<label>Text<textarea id="caseInput" rows="8"></textarea></label><label>Case<select id="caseType"><option value="upper">UPPERCASE</option><option value="lower">lowercase</option><option value="title">Title Case</option><option value="sentence">Sentence case</option></select></label><button data-action=\"convertCase\">Convert</button><div id="result" class="result"></div>',password:'<label>Length<input id="pwLength" type="number" min="8" max="128" value="20"></label><button data-action=\"generatePassword\">Generate</button><div id="result" class="result"></div>',duration:'<label>Start time<input id="startTime" type="time" value="09:00"></label><label>End time<input id="endTime" type="time" value="17:30"></label><button data-action=\"calcDuration\">Calculate</button><div id="result" class="result"></div>',businessdays:'<label>Start date<input id="bdStart" type="date"></label><label>End date<input id="bdEnd" type="date"></label><button data-action=\"calcBusinessDays\">Calculate</button><div id="result" class="result"></div>',compound:'<label>Principal<input id="cp" type="number" value="1000"></label><label>Annual rate (%)<input id="cr" type="number" value="8"></label><label>Years<input id="cy" type="number" value="5"></label><label>Compounds per year<input id="cm" type="number" value="12"></label><button data-action=\"calcCompound\">Calculate</button><div id="result" class="result"></div>',margin:'<label>Revenue<input id="rev" type="number" value="1000"></label><label>Total cost<input id="cost" type="number" value="700"></label><button data-action=\"calcMargin\">Calculate</button><div id="result" class="result"></div>',roi:'<label>Initial investment<input id="inv" type="number" value="1000"></label><label>Final value<input id="ret" type="number" value="1250"></label><button data-action=\"calcRoi\">Calculate</button><div id="result" class="result"></div>',breakeven:'<label>Fixed costs<input id="fixed" type="number" value="5000"></label><label>Selling price per unit<input id="sell" type="number" value="50"></label><label>Variable cost per unit<input id="variable" type="number" value="30"></label><button data-action=\"calcBreakEven\">Calculate</button><div id="result" class="result"></div>',"image-compressor":"<label>Image<input id=\"imgFile\" type=\"file\" accept=\"image/*\"></label><label>Quality<input id=\"imgQuality\" type=\"range\" min=\"0.1\" max=\"1\" step=\"0.05\" value=\"0.8\"></label><button type=\"button\" data-action=\"imageCompress\">Compress & Download</button><div id=\"result\" class=\"result\"></div>","image-resizer":"<label>Image<input id=\"resizeFile\" type=\"file\" accept=\"image/*\"></label><label>Width (px)<input id=\"resizeWidth\" type=\"number\" min=\"1\" value=\"1200\"></label><button type=\"button\" data-action=\"imageResize\">Resize & Download</button><div id=\"result\" class=\"result\"></div>","image-converter":"<label>Image<input id=\"convertFile\" type=\"file\" accept=\"image/*\"></label><label>Output format<select id=\"convertFormat\"><option value=\"image/jpeg\">JPEG</option><option value=\"image/png\">PNG</option><option value=\"image/webp\">WebP</option></select></label><button type=\"button\" data-action=\"imageConvert\">Convert & Download</button><div id=\"result\" class=\"result\"></div>","randomNumber":"<h2>Random Number Generator</h2><p class=\"note\">Generate a cryptographically strong random integer in your browser.</p><label>Minimum<input id=\"rndMin\" type=\"number\" value=\"1\"></label><label>Maximum<input id=\"rndMax\" type=\"number\" value=\"100\"></label><button type=\"button\" data-action=\"generateRandom\">Generate</button><div id=\"result\" class=\"result\"></div>","aspectRatio":"<h2>Aspect Ratio Calculator</h2><label>Width<input id=\"arw\" type=\"number\" value=\"1920\"></label><label>Height<input id=\"arh\" type=\"number\" value=\"1080\"></label><button type=\"button\" data-action=\"calcAspect\">Calculate</button><div id=\"result\" class=\"result\"></div>","timestamp":"<h2>Unix Timestamp Converter</h2><label>Unix timestamp<input id=\"ts\" type=\"number\" placeholder=\"e.g. 1750000000\"></label><label>Input unit<select id=\"tsUnit\"><option value=\"seconds\">Seconds</option><option value=\"milliseconds\">Milliseconds</option><option value=\"auto\">Auto-detect</option></select></label><button type=\"button\" data-action=\"timestampToDate\">Convert timestamp</button><label>Date and time<input id=\"dt\" type=\"datetime-local\"></label><button type=\"button\" data-action=\"dateToTimestamp\">Convert date</button><div id=\"result\" class=\"result\"></div>","quotePulse":"<h2>QuotePulse</h2><p class=\"note\">Paste a quote or estimate. This checks document quality and obvious arithmetic issues locally in your browser. It does not determine whether a price is fair.</p><label>Quote or estimate<textarea id=\"quoteText\" rows=\"14\" placeholder=\"Example:\nSite visit $50\nLabour $480\nMaterials $620\nDeposit 60%\nTotal $1,150\n12-month warranty\"></textarea></label><div class=\"quote-actions\"><button type=\"button\" class=\"secondary\" data-action=\"quotePulseExample\">Load example</button><button type=\"button\" data-action=\"analyzeQuote\">Analyze quote</button><button type=\"button\" class=\"secondary\" data-action=\"print\">Print / save PDF</button></div><div id=\"result\" class=\"result\"></div><div id=\"qpMessage\" class=\"result\" hidden></div>","base64":"<h2>Base64 Encoder & Decoder</h2><label>Text<textarea id=\"b64\" rows=\"7\" placeholder=\"Enter text\"></textarea></label><button type=\"button\" data-action=\"encodeBase64\">Encode</button><button type=\"button\" data-action=\"decodeBase64\">Decode</button><div id=\"result\" class=\"result\"></div>"};if(!views[type]){el.textContent="Tool unavailable.";return}el.innerHTML='<div class="form">'+views[type]+'</div>'}

function fmt(v){return Number.isFinite(v)?v.toLocaleString(undefined,{maximumFractionDigits:2}):"Invalid result"}
function calcPercentage(){const partValue=$("part").value.trim(),wholeValue=$("whole").value.trim();if(!partValue||!wholeValue){$("result").textContent="Enter valid numbers.";return}const p=Number(partValue),w=Number(wholeValue);if(!Number.isFinite(p)||!Number.isFinite(w)){$("result").textContent="Enter valid numbers.";return}if(w===0){$("result").textContent="Total must not be zero.";return}const result=p/w*100;if(!Number.isFinite(result)){$("result").textContent="These values are too large for a reliable percentage.";return}$("result").textContent=fmt(result===0?0:result)+"%"}
function calcDiscount(){const p=n("price"),d=n("disc");if(!Number.isFinite(p)||!Number.isFinite(d)||p<0||d<0||d>100){$("result").textContent="Enter a valid price and discount from 0 to 100%.";return}const save=p*d/100,finalPrice=p-save;if(!Number.isFinite(save)||!Number.isFinite(finalPrice)){$("result").textContent="These values are too large for a reliable discount result.";return}$("result").innerHTML="<strong>Final price: "+fmt(finalPrice)+"</strong><br>You save "+fmt(save)+"."}

function esc(v){return String(v).replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[ch]))}
function calcLoan(){const p=n("loanAmount"),annual=n("loanRate"),months=n("loanMonths");if(!Number.isFinite(p)||!Number.isFinite(annual)||!Number.isFinite(months)||p<=0||months<=0||annual<0||!Number.isSafeInteger(months)){$("result").textContent="Enter a positive loan amount, non-negative rate, and whole number of months.";return}const r=annual/1200,pay=r?p*r*Math.pow(1+r,months)/(Math.pow(1+r,months)-1):p/months,total=pay*months;if(!Number.isFinite(pay)||!Number.isFinite(total)){$("result").textContent="These values are too large for a reliable loan result.";return}$("result").innerHTML='<strong>'+fmt(pay)+' per month</strong><br>Total payment: '+fmt(total)+'<br>Total interest: '+fmt(total-p)}
function calcInterest(){const p=n("principal"),r=n("rate")/100,y=n("years");if(!Number.isFinite(p)||!Number.isFinite(r)||!Number.isFinite(y)||p<0||r<0||y<0){$("result").textContent="Enter valid non-negative values.";return}const simple=p*r*y,compound=p*Math.pow(1+r,y)-p;if(!Number.isFinite(simple)||!Number.isFinite(compound)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML='<strong>Simple interest: '+fmt(simple)+'</strong><br>Compound interest: '+fmt(compound)}
function calcConvert(){const v=n("uv"),a=$("uf").value,b=$("ut").value;if(!Number.isFinite(v)){$("result").textContent="Enter a valid number.";return}const valid=["km","mi","kg","lb","c","f"];if(!valid.includes(a)||!valid.includes(b)){$("result").textContent="Those units are not supported.";return}if(v<0&&(a==="km"||a==="mi"||a==="kg"||a==="lb")){$("result").textContent="Length and weight values cannot be negative.";return}let x;if(a===b)x=v;else if(a==="km"&&b==="mi")x=v*0.621371;else if(a==="mi"&&b==="km")x=v/0.621371;else if(a==="kg"&&b==="lb")x=v*2.2046226218;else if(a==="lb"&&b==="kg")x=v/2.2046226218;else if(a==="c"&&b==="f")x=v*9/5+32;else if(a==="f"&&b==="c")x=(v-32)*5/9;else{$("result").textContent="Choose compatible units.";return}if(!Number.isFinite(x)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML="<strong>"+fmt(x)+"</strong> "+esc(b.toUpperCase())}
function parseIsoDate(value){if(!/^\d{4}-\d{2}-\d{2}$/.test(value))return null;const [year,month,day]=value.split("-").map(Number);const d=new Date(Date.UTC(year,month-1,day));return d.getUTCFullYear()===year&&d.getUTCMonth()===month-1&&d.getUTCDate()===day?d:null}
function calcAge(){const s=$("dob").value;if(!s){$("result").textContent="Enter a date of birth.";return}const d=parseIsoDate(s),today=new Date();today.setHours(0,0,0,0);if(!d){$("result").textContent="Enter a valid date of birth.";return}if(d>today){$("result").textContent="Date of birth cannot be in the future.";return}let y=today.getFullYear()-d.getFullYear(),m=today.getMonth()-d.getMonth(),day=today.getDate()-d.getDate();if(day<0){m--;day+=new Date(today.getFullYear(),today.getMonth(),0).getDate()}if(m<0){y--;m+=12}$("result").innerHTML='<strong>'+y+' years</strong>, '+m+' months and '+day+' days.'}
function calcChange(){const a=n("oldv"),b=n("newv");if(!Number.isFinite(a)||!Number.isFinite(b)||a===0){$("result").textContent="Enter valid values. Original value must not be zero.";return}const change=((b-a)/Math.abs(a))*100;if(!Number.isFinite(change)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML="<strong>"+change.toFixed(2)+"%</strong> change."}
function calcTip(){const b=n("bill"),t=n("tipRate"),p=n("people");if(!Number.isFinite(b)||!Number.isFinite(t)||!Number.isSafeInteger(p)||b<0||t<0||p<1){$("result").textContent="Enter a valid bill, non-negative tip, and whole number of people.";return}const tip=b*t/100,total=b+tip;if(!Number.isFinite(tip)||!Number.isFinite(total)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML="<strong>"+(total/p).toFixed(2)+" per person</strong><br>Tip: "+tip.toFixed(2)+"<br>Total: "+total.toFixed(2)}
function calcBmi(){const h=n("cm")/100,w=n("kg");if(!Number.isFinite(h)||!Number.isFinite(w)||h<=0||w<=0){$("result").textContent="Enter positive height and weight.";return}const bmi=w/(h*h);if(!Number.isFinite(bmi)){$("result").textContent="These values are too large for a reliable result.";return}const category=bmi<18.5?"Underweight":bmi<25?"Healthy weight":bmi<30?"Overweight":"Obesity";const position=Math.min(100,Math.max(0,(bmi-15)/25*100));const minWeight=18.5*h*h,maxWeight=24.9*h*h;$("result").innerHTML="<div class=\"bmi-summary\"><div><span class=\"result-label\">BMI</span><strong>"+bmi.toFixed(1)+"</strong><span class=\"bmi-category\">"+category+"</span></div><div class=\"bmi-gauge\" role=\"img\" aria-label=\"BMI scale\"><div class=\"bmi-track\"><span class=\"bmi-zone underweight\"></span><span class=\"bmi-zone healthy\"></span><span class=\"bmi-zone overweight\"></span><span class=\"bmi-zone obesity\"></span><i class=\"bmi-marker\" style=\"left:"+position.toFixed(1)+"%\"></i></div><div class=\"bmi-scale-labels\"><span>15</span><span>18.5</span><span>25</span><span>30</span><span>40+</span></div></div></div><div class=\"bmi-details\"><div><span>Standard healthy-range weight for this height</span><strong>"+minWeight.toFixed(1)+"-"+maxWeight.toFixed(1)+" kg</strong></div><div><span>Category</span><strong>"+category+"</strong></div></div><p class=\"note\"><strong>Important:</strong> BMI is a screening measure for adults, not a diagnosis. It does not directly measure body fat and may not reflect health status for every person. Children, teens, pregnancy and some athletes require different interpretation.</p>"}
function calcDate(){const a=$("d1").value,b=$("d2").value;if(!a||!b){$("result").textContent="Enter both dates.";return}const start=parseIsoDate(a),end=parseIsoDate(b);if(!start||!end){$("result").textContent="Enter valid dates.";return}const days=Math.abs((end-start)/86400000);$("result").innerHTML="<strong>"+days+" days</strong> between the selected dates."}

function calcText(){const s=$("tc").value,words=s.trim()?s.trim().split(/\s+/).length:0,characters=Array.from(s).length;$("result").innerHTML="<strong>"+words+" words</strong><br>"+characters+" characters."}

function calcCompound(){const p=n("cp"),r=n("cr")/100,y=n("cy"),m=n("cm");if(!Number.isFinite(p)||!Number.isFinite(r)||!Number.isFinite(y)||!Number.isFinite(m)||p<0||r<0||y<0||m<1||!Number.isSafeInteger(m)){$("result").textContent="Enter valid values.";return}const exponent=m*y,a=p*Math.pow(1+r/m,exponent);if(!Number.isFinite(a)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML="<strong>Final balance: "+a.toFixed(2)+"</strong><br>Interest earned: "+(a-p).toFixed(2)}
function calcMargin(){const rev=n("rev"),cost=n("cost");if(!Number.isFinite(rev)||!Number.isFinite(cost)||rev<=0||cost<0){$("result").textContent="Enter valid revenue and cost.";return}const profit=rev-cost,margin=profit/rev*100;if(!Number.isFinite(profit)||!Number.isFinite(margin)){$("result").textContent="These values are too large for a reliable result.";return}const markup=cost?profit/cost*100:null;$("result").innerHTML="<strong>Profit: "+profit.toFixed(2)+"</strong><br>Profit margin: "+margin.toFixed(2)+"%<br>Markup on cost: "+(markup===null?"undefined (cost is zero)":markup.toFixed(2)+"%")}
function calcRoi(){const inv=n("inv"),ret=n("ret");if(!Number.isFinite(inv)||!Number.isFinite(ret)||inv<=0||ret<0){$("result").textContent="Enter valid values.";return}const gain=ret-inv,roi=gain/inv*100;if(!Number.isFinite(gain)||!Number.isFinite(roi)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML="<strong>ROI: "+roi.toFixed(2)+"%</strong><br>Net gain: "+gain.toFixed(2)}
function calcBreakEven(){const fixed=n("fixed"),price=n("sell"),variable=n("variable");if(!Number.isFinite(fixed)||!Number.isFinite(price)||!Number.isFinite(variable)||fixed<0||price<0||variable<0||price<=variable){$("result").textContent="Enter valid costs. Selling price must be greater than variable cost.";return}const units=fixed/(price-variable),revenue=units*price;if(!Number.isFinite(units)||!Number.isFinite(revenue)){$("result").textContent="These values are too large for a reliable result.";return}$("result").innerHTML="<strong>"+units.toFixed(2)+" units</strong><br>Break-even revenue: "+revenue.toFixed(2)}


function downloadBlob(blob,name){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(url)},1000)}
function loadImage(input){return new Promise(function(resolve,reject){const file=input instanceof File?input:(input.files&&input.files[0]);if(!file){reject(new Error('Choose an image.'));return}if(file.size>25*1024*1024){reject(new Error('For reliable phone performance, choose an image under 25 MB.'));return}const url=URL.createObjectURL(file),im=new Image();const cleanup=function(){URL.revokeObjectURL(url)};im.onload=function(){cleanup();resolve({im:im,file:file})};im.onerror=function(){cleanup();reject(new Error('Could not read that image.'))};im.src=url})}
async function imageCompress(){try{const f=$("imgFile").files?.[0],quality=Number($("imgQuality").value);if(!f){$("result").textContent="Choose an image first.";return}if(f.size>25*1024*1024){$("result").textContent="For reliable browser performance, choose an image under 25 MB.";return}if(!Number.isFinite(quality)||quality<0||quality>1){$("result").textContent="Choose a quality between 0 and 1.";return}loadImage(f).then(x=>{const w=x.im.naturalWidth,h=x.im.naturalHeight;if(!Number.isSafeInteger(w)||!Number.isSafeInteger(h)||w<1||h<1||w>16384||h>16384){$("result").textContent="The image dimensions are too large for reliable browser processing.";return}const cv=document.createElement("canvas");cv.width=w;cv.height=h;const ctx=cv.getContext("2d");if(!ctx){$("result").textContent="Canvas processing is unavailable in this browser.";return}ctx.drawImage(x.im,0,0);cv.toBlob(b=>{if(!b){$("result").textContent="Could not create output.";return}downloadBlob(b,"compressed.jpg");$("result").textContent="Done. Output: "+fmt(b.size/1024)+" KB."},"image/jpeg",quality)}).catch(e=>$("result").textContent=e.message)}catch(e){$("result").textContent=e.message}}
async function imageResize(){try{const f=$("resizeFile").files?.[0],w=n("resizeWidth");if(!f){$("result").textContent="Choose an image first.";return}if(f.size>25*1024*1024){$("result").textContent="For reliable browser performance, choose an image under 25 MB.";return}if(!Number.isFinite(w)||!Number.isInteger(w)||w<1||w>16384){$("result").textContent="Enter a whole-number width from 1 to 16384 px.";return}const x=await loadImage(f);if(!x.im.naturalWidth||!x.im.naturalHeight){$("result").textContent="Could not determine the image dimensions.";return}const h=Math.round(x.im.naturalHeight*w/x.im.naturalWidth);if(!Number.isSafeInteger(h)||h<1||h>16384){$("result").textContent="The requested size is too large for reliable browser processing.";return}const outputType=f.type==="image/png"?"image/png":"image/jpeg",extension=outputType==="image/png"?"png":"jpg";const cv=document.createElement("canvas");cv.width=w;cv.height=h;const ctx=cv.getContext("2d");if(!ctx){$("result").textContent="Canvas processing is unavailable in this browser.";return}ctx.drawImage(x.im,0,0,w,h);cv.toBlob(b=>{if(!b){$("result").textContent="Could not create output.";return}downloadBlob(b,"resized."+extension);$("result").textContent="Done: "+w+" x "+h+" px."},outputType,.9)}catch(e){$("result").textContent=e.message}}
async function imageConvert(){try{const f=$("convertFile").files?.[0],type=$("convertFormat").value,allowed=["image/jpeg","image/png","image/webp"];if(!f){$("result").textContent="Choose an image first.";return}if(f.size>25*1024*1024){$("result").textContent="For reliable browser performance, choose an image under 25 MB.";return}if(!allowed.includes(type)){$("result").textContent="Choose a supported output format.";return}const x=await loadImage(f),w=x.im.naturalWidth,h=x.im.naturalHeight;if(!Number.isSafeInteger(w)||!Number.isSafeInteger(h)||w<1||h<1||w>16384||h>16384){$("result").textContent="The image dimensions are too large for reliable browser processing.";return}const ext=type==="image/jpeg"?"jpg":type.split("/")[1],cv=document.createElement("canvas"),ctx=cv.getContext("2d");if(!ctx){$("result").textContent="Canvas processing is unavailable in this browser.";return}cv.width=w;cv.height=h;ctx.drawImage(x.im,0,0);cv.toBlob(b=>{if(!b||b.type!==type){$("result").textContent="This browser could not produce the requested output format.";return}downloadBlob(b,"converted."+ext);$("result").textContent="Converted successfully."},type,.9)}catch(e){$("result").textContent=e.message}}
function formatJson(){try{const v=JSON.parse($("jsonInput").value);$("jsonInput").value=JSON.stringify(v,null,2);$("result").textContent="Valid JSON."}catch(e){$("result").textContent="Invalid JSON: "+e.message}}
function convertCase(){const s=$("caseInput").value,t=$("caseType").value;let o=s;if(t==="upper")o=s.toUpperCase();else if(t==="lower")o=s.toLowerCase();else if(t==="title")o=s.toLowerCase().replace(/\b\w/g,function(x){return x.toUpperCase()});else o=s.toLowerCase().replace(/(^|[.!?]\s+)\w/g,function(x){return x.toUpperCase()});$("caseInput").value=o;$("result").textContent="Converted."}
function generatePassword(){const chars="ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz23456789!@#$%^&*()_+-=[]{}";const len=Math.min(128,Math.max(8,parseInt($("pwLength").value,10)||20)),r=$("result");if(!window.crypto||!crypto.getRandomValues){r.textContent="Secure password generation is not available in this browser.";return}const a=new Uint32Array(len),limit=Math.floor(4294967296/chars.length)*chars.length;let p="";for(let i=0;i<len;i++){let x;do{crypto.getRandomValues(a.subarray(i,i+1));x=a[i]}while(x>=limit);p+=chars[x%chars.length]}r.innerHTML="<strong>"+esc(p)+"</strong><br>Generated locally. Do not reuse passwords."}
function calcDuration(){const a=$("startTime").value,b=$("endTime").value;if(!a||!b){$("result").textContent="Enter both times.";return}const x=a.split(":").map(Number),y=b.split(":").map(Number);if(x.length!==2||y.length!==2||x.some(v=>!Number.isFinite(v))||y.some(v=>!Number.isFinite(v))||x[0]<0||x[0]>23||y[0]<0||y[0]>23||x[1]<0||x[1]>59||y[1]<0||y[1]>59){$("result").textContent="Enter valid times.";return}let m=y[0]*60+y[1]-x[0]*60-x[1];if(m<0)m+=1440;$("result").innerHTML="<strong>"+Math.floor(m/60)+"h "+m%60+"m</strong>"}
function calcBusinessDays(){const a=$("bdStart").value,b=$("bdEnd").value;if(!a||!b){$("result").textContent="Enter both dates.";return}let s=parseIsoDate(a),e=parseIsoDate(b);if(!s||!e){$("result").textContent="Enter valid dates.";return}if(s>e){const t=s;s=e;e=t}const days=Math.floor((e-s)/86400000)+1,fullWeeks=Math.floor(days/7),remainder=days%7,startDay=s.getUTCDay();let count=fullWeeks*5;for(let i=0;i<remainder;i++){const day=(startDay+i)%7;if(day!==0&&day!==6)count++}$("result").innerHTML="<strong>"+count+" weekdays</strong><br>Public holidays are not excluded."}

function randomNumber(){return '<h2>Random Number Generator</h2><p class="note">Generate a cryptographically strong random integer in your browser.</p><div class="form"><label>Minimum<input id="rndMin" type="number" value="1"></label><label>Maximum<input id="rndMax" type="number" value="100"></label><button type="button" data-action=\"generateRandom\">Generate</button></div><div id="result" class="result"></div>'}
function generateRandom(){const min=Number(el("rndMin").value),max=Number(el("rndMax").value),r=el("result");if(!Number.isSafeInteger(min)||!Number.isSafeInteger(max)||min>max){r.textContent="Enter whole numbers with minimum no greater than maximum.";return}if(!window.crypto||typeof crypto.getRandomValues!=="function"){r.textContent="Secure random generation is not available in this browser.";return}const range=BigInt(max)-BigInt(min)+1n;if(range<=0n||range>4294967296n){r.textContent="Use a range of up to 4,294,967,296 possible integers.";return}const limit=4294967296n-(4294967296n%range),buf=new Uint32Array(1);let x;do{crypto.getRandomValues(buf);x=BigInt(buf[0])}while(x>=limit);r.innerHTML='<strong>'+ (BigInt(min)+(x%range)).toString() +'</strong>'}
function aspectRatio(){return '<h2>Aspect Ratio Calculator</h2><div class="form"><label>Width<input id="arw" type="number" value="1920"></label><label>Height<input id="arh" type="number" value="1080"></label><button type="button" data-action=\"calcAspect\">Calculate</button></div><div id="result" class="result"></div>'}
function calcAspect(){const w=Number(el("arw").value),h=Number(el("arh").value),r=el("result");if(!Number.isSafeInteger(w)||!Number.isSafeInteger(h)||w<=0||h<=0){r.textContent="Enter positive whole-number width and height.";return}function gcd(a,b){while(b){const t=a%b;a=b;b=t}return a}const g=gcd(w,h);r.innerHTML='<strong>'+(w/g)+':'+(h/g)+'</strong> aspect ratio.'}
function timestamp(){return '<h2>Unix Timestamp Converter</h2><div class="form"><label>Unix timestamp<input id="ts" type="number" placeholder="e.g. 1750000000"></label><label>Timestamp unit<select id="tsUnit"><option value="seconds">Seconds</option><option value="milliseconds">Milliseconds</option><option value="auto">Auto-detect</option></select></label><button type="button" data-action=\"timestampToDate\">Convert timestamp</button><label>Date and time<input id="dt" type="datetime-local"></label><button type="button" data-action=\"dateToTimestamp\">Convert date</button></div><div id="result" class="result"></div>'}
function timestampToDate(){const v=Number(el("ts").value),unit=el("tsUnit").value,r=el("result");if(!["seconds","milliseconds","auto"].includes(unit)){r.textContent="Choose a supported timestamp unit.";return}if(!Number.isSafeInteger(v)){r.textContent="Enter a whole-number timestamp within the supported safe range.";return}const ms=unit==="milliseconds"?v:unit==="auto"?(Math.abs(v)>=100000000000?v:v*1000):v*1000;if(!Number.isSafeInteger(ms)){r.textContent="Timestamp is outside the supported safe range.";return}const d=new Date(ms);if(Number.isNaN(d.getTime())){r.textContent="Timestamp is outside the supported date range.";return}r.textContent=d.toISOString()}
function dateToTimestamp(){const v=el("dt").value,r=el("result");if(!v){r.textContent="Choose a date and time.";return}const d=new Date(v);if(Number.isNaN(d.getTime())){r.textContent="Enter a valid date and time.";return}r.textContent=Math.floor(d.getTime()/1000).toString()}
function base64(){return '<h2>Base64 Encoder & Decoder</h2><div class="form"><label>Text<textarea id="b64" rows="7" placeholder="Enter text"></textarea></label><button type="button" data-action=\"encodeBase64\">Encode</button><button type="button" data-action=\"decodeBase64\">Decode</button></div><div id="result" class="result"></div>'}
function encodeBase64(){const s=el("b64").value,r=el("result");try{const bytes=new TextEncoder().encode(s);let bin="";bytes.forEach(b=>bin+=String.fromCharCode(b));r.textContent=btoa(bin)}catch(e){r.textContent="Could not encode this text."}}
function decodeBase64(){const s=el("b64").value.trim(),r=el("result");try{const bin=atob(s),bytes=Uint8Array.from(bin,c=>c.charCodeAt(0));r.textContent=new TextDecoder("utf-8",{fatal:true}).decode(bytes)}catch(e){r.textContent="Invalid Base64 input."}}


function quotePulseExample(){
  const example = "Site visit $50\nLabour $480\nMaterials $620\nDeposit 60%\nTotal $1150\n12-month warranty";
  const field = el("quoteText");
  if(field){field.value=example}
}
function analyzeQuote(){
  const raw = (el("quoteText")?.value || "").trim();
  const r = el("result"), msg = el("qpMessage");
  if(!raw){r.textContent="Paste a quote or estimate first.";if(msg)msg.hidden=true;return}
  const lines = raw.split(/\r?\n/).map(x=>x.trim()).filter(Boolean);
  const amountRe = /(?:[$€£₹]\s*|\b(?:USD|EUR|GBP|INR|CAD|AUD)\s*)?(\d{1,3}(?:,\d{3})+(?:\.\d+)?|\d+(?:\.\d+)?)(?!\s*%)/gi;
  const lineItems = [];
  let statedTotal = null, depositPercent = null, depositAmount = null;
  const lower = raw.toLowerCase();
  lines.forEach(line=>{
    const l=line.toLowerCase();
    const percentMatch=l.match(/(?:deposit|down payment|upfront)[^\d%]*(\d+(?:\.\d+)?)\s*%/i);
    if(percentMatch) depositPercent=Number(percentMatch[1]);
    const isTotal=/\b(?:grand total|total|amount due|balance due)\b/i.test(line);
    const isDeposit=/\b(?:deposit|down payment|upfront)\b/i.test(line);
    const totalMatch=line.match(/\b(?:grand total|total|amount due|balance due)\b[^\d$€£₹]{0,24}[$€£₹]?\s*([\d,]+(?:\.\d+)?)/i);
    if(totalMatch) statedTotal=Number(totalMatch[1].replace(/,/g,""));
    const matches=[...line.matchAll(amountRe)];
    if(matches.length){
      const m=matches[matches.length-1];
      const value=Number(m[1].replace(/,/g,""));
      const hasPercent=/%/.test(line.slice(Math.max(0,m.index-8), Math.min(line.length,m.index+m[0].length+8)));
      const dateLike=/\b20\d{2}[-/]\d{1,2}[-/]\d{1,2}\b/.test(line);
      const durationLike=/\b\d+(?:\.\d+)?\s*(?:second|seconds|sec|secs|minute|minutes|min|mins|hour|hours|hr|hrs|day|days|week|weeks|month|months|year|years)\b/i.test(line);
      const quantityLike=/\b\d+(?:\.\d+)?\s*(?:item|items|unit|units|piece|pieces|pcs|qty|quantity)\b/i.test(line);
      if(Number.isFinite(value)&&value>=0&&!hasPercent&&!dateLike&&!durationLike&&!quantityLike&&!isTotal&&!isDeposit){
        let label=line.slice(0,m.index).replace(/[-:|•]+\s*$/,"").trim();
        label=label.replace(/^(item|description|service|line)\s*[:#-]?\s*/i,"").trim();
        if(label.length>1) lineItems.push({label,value});
      } else if(isDeposit&&Number.isFinite(value)&&value>0){depositAmount=value}
    }
  });
  const flags=[];
  if(statedTotal===null) flags.push({type:"watch",text:"No explicit total was detected. Ask for the final amount including taxes, fees, and extras."});
  if(lineItems.length===0) flags.push({type:"watch",text:"No clear line-item amounts were detected. Ask for an itemized breakdown."});
  if(depositPercent!==null){
    if(depositPercent>50) flags.push({type:"risk",text:"The stated deposit is above 50%. Confirm what is refundable and what happens if the job is cancelled."});
    else if(depositPercent>30) flags.push({type:"watch",text:"The quote asks for more than 30% upfront. Check payment milestones and refund terms."});
  } else if(depositAmount!==null&&statedTotal>0){
    depositPercent=depositAmount/statedTotal*100;
    if(depositPercent>50) flags.push({type:"risk",text:"The detected deposit is above 50% of the stated total. Confirm refund and cancellation terms."});
  }
  if(/\b(misc|miscellaneous|other|sundries|etc\.?|tbd|allowance|provisional|as required|contingency)\b/i.test(raw))
    flags.push({type:"watch",text:"Vague scope wording was detected. Ask exactly what is included, excluded, and billable as a change order."});
  if(!/\b(warranty|guarantee)\b/i.test(lower))
    flags.push({type:"watch",text:"No warranty or guarantee language was detected. Ask for duration, coverage, and exclusions."});
  if(!/\b(change order|variation|extra work|additional work|scope change)\b/i.test(lower))
    flags.push({type:"watch",text:"No change-order rule was detected. Ask how extra work is approved and priced."});
  if(!/\b(cancel|cancellation|refund|refundable)\b/i.test(lower))
    flags.push({type:"watch",text:"No cancellation or refund terms were detected."});
  if(!/\b(tax|vat|gst|sales tax|service tax|fees?)\b/i.test(lower))
    flags.push({type:"watch",text:"Taxes or fees are not mentioned. Confirm whether the quoted total is all-in."});
  if(statedTotal!==null&&lineItems.length){
    const sum=lineItems.reduce((a,x)=>a+x.value,0);
    const diff=sum-statedTotal, relative=statedTotal?Math.abs(diff)/statedTotal:0;
    if(relative>0.01) flags.push({type:"risk",text:"The detected line items do not reconcile with the stated total. Ask for the exact source of the difference."});
  }
  const counts={};
  lineItems.forEach(x=>{
    const key=x.label.toLowerCase().replace(/[^a-z0-9]+/g," ").trim();
    if(key) counts[key]=(counts[key]||0)+1;
  });
  const duplicateLabels=Object.keys(counts).filter(k=>counts[k]>1);
  if(duplicateLabels.length) flags.push({type:"watch",text:"Duplicate-looking line items were detected. Ask whether they are separate charges or accidental repeats."});
  let score=100;
  flags.forEach(f=>score-=f.type==="risk"?15:8);
  if(score<20) score=20;
  const level=score>=80?"Clear":score>=60?"Needs questions":"Needs careful review";
  const itemSum=lineItems.reduce((a,x)=>a+x.value,0);
  const currencyHint=(raw.match(/[$€£₹]|\b(?:USD|EUR|GBP|INR|CAD|AUD)\b/i)||[""])[0];
  const fmtMoney=v=>Number.isFinite(v)?v.toLocaleString(undefined,{maximumFractionDigits:2}):"n/a";
  const flagHtml=flags.length?flags.map(f=>'<div class="quote-flag '+esc(f.type)+'"><strong>'+esc(f.type==="risk"?"Review":"Check")+'</strong><span>'+esc(f.text)+'</span></div>').join(""):'<div class="quote-good">No obvious document-level red flags were detected.</div>';
  const itemsHtml=lineItems.slice(0,12).map(x=>'<div class="quote-row"><span>'+esc(x.label)+'</span><b>'+esc(currencyHint)+' '+fmtMoney(x.value)+'</b></div>').join("");
  r.innerHTML='<div class="quote-grid"><div class="quote-metric"><span>Clarity score</span><strong>'+score+'/100</strong><small>'+level+'</small></div><div class="quote-metric"><span>Line items</span><strong>'+lineItems.length+'</strong><small>detected</small></div><div class="quote-metric"><span>Stated total</span><strong>'+(statedTotal!==null?esc(currencyHint)+' '+fmtMoney(statedTotal):"Not found")+'</strong><small>from text</small></div><div class="quote-metric"><span>Detected item sum</span><strong>'+(lineItems.length?esc(currencyHint)+' '+fmtMoney(itemSum):"Not found")+'</strong><small>before non-item lines</small></div></div><h3>What to check</h3>'+flagHtml+(itemsHtml?'<h3>Detected line items</h3><div class="quote-list">'+itemsHtml+'</div>':"")+'<p class="note"><strong>Important:</strong> QuotePulse checks document completeness and arithmetic patterns. It does not know local market prices and does not replace professional, legal, financial, or trade advice.</p>';
  if(msg){
    const questions=[
      "Can you confirm exactly what is included and excluded in each line item?",
      "Is the quoted total final, including taxes, fees, delivery, permits, and expected extras?",
      depositPercent!==null?"What happens to the deposit if the work is cancelled or scope changes?":"What are the payment milestones and refund terms?",
      "What warranty or guarantee is included, and for how long?",
      "How are extra or change-order costs approved before they are billed?"
    ];
    msg.hidden=false;
    msg.innerHTML='<h3>Negotiation message</h3><p>Hi, thanks for the quote. Before I approve it, could you please confirm the following?</p><ol>'+questions.map(q=>'<li>'+esc(q)+'</li>').join("")+'</ol><p>Once I have those details, I can review the scope and payment terms clearly.</p><button type="button" data-action=\"copyQuoteMessage\">Copy message</button>';
    const messageText="Hi, thanks for the quote. Before I approve it, could you please confirm the following?\n\n"+questions.map((q,i)=>(i+1)+". "+q).join("\n")+"\n\nOnce I have those details, I can review the scope and payment terms clearly.";
    if(msg.dataset) msg.dataset.message=messageText;
  }
}
function copyQuoteMessage(){
  const box=el("qpMessage"),text=box?.dataset?.message||"";
  if(!text)return;
  if(navigator.clipboard&&navigator.clipboard.writeText){navigator.clipboard.writeText(text).then(()=>{box.insertAdjacentHTML("beforeend",'<p class="note">Copied.</p>')}).catch(()=>{prompt("Copy this message:",text)})}
  else prompt("Copy this message:",text);
}



const toolActions=Object.freeze({calcPercentage,calcDiscount,calcLoan,calcInterest,calcAge,calcChange,calcTip,calcBmi,calcDate,calcConvert,calcText,formatJson,convertCase,generatePassword,calcDuration,calcBusinessDays,calcCompound,calcMargin,calcRoi,calcBreakEven,imageCompress,imageResize,imageConvert,generateRandom,calcAspect,timestampToDate,dateToTimestamp,analyzeQuote,quotePulseExample,print:()=>window.print(),encodeBase64,decodeBase64,copyQuoteMessage});
document.addEventListener("click",event=>{const button=event.target.closest("[data-action]");if(!button)return;const action=button.dataset.action;const fn=toolActions[action];if(typeof fn==="function")fn();});
